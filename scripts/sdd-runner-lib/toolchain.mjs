import{createHash}from'node:crypto';const V=/^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(?:-[0-9A-Za-z-]+(?:\.[0-9A-Za-z-]+)*)?(?:\+[0-9A-Za-z-]+(?:\.[0-9A-Za-z-]+)*)?$/;
export function validateToolchainLock(l){if(!l||l.version!=='toolchain-lock/v1'||!l.tools||typeof l.tools!=='object')throw Error('toolchain_lock_invalid');const tools={};for(const[id,t]of Object.entries(l.tools)){if(!t||typeof t.provider!=='string'||!t.provider.trim()||typeof t.version!=='string'||!V.test(t.version)||!digest(t.digest)||(t.commit!==undefined&&!/^[0-9a-f]{7,64}$/i.test(t.commit)))throw Error(`toolchain_unpinned:${id}`);tools[id]={...t}}return{version:'toolchain-lock/v1',tools}}
export const toolchainDigest=l=>createHash('sha256').update(stable(validateToolchainLock(l))).digest('hex');export const assertPinnedTool=t=>{validateToolchainLock({version:'toolchain-lock/v1',tools:{tool:t}});return true};function digest(v){return typeof v==='string'&&/^(sha256:)?[0-9a-f]{64}$/i.test(v)}function stable(v){if(Array.isArray(v))return`[${v.map(stable).join(',')}]`;if(v&&typeof v==='object')return`{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${stable(v[k])}`).join(',')}}`;return JSON.stringify(v)}
