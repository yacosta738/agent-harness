{
  "_comment": "Documents the redaction contract: secret tokens leaked through stdout must be redacted in the persisted envelope before writeEvidence. The smoke script builds the runtime object from process context, exercising redactValue through createRedactor with literal value 'TOPSECRET-XYZ' and pattern 'AKIA[0-9A-Z]{16}'."
}